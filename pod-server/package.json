{
  "name": "autofound-pod-server",
  "version": "1.0.0",
  "description": "Agent pod server — runs inside each Fly.io machine",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "fastify": "^5.2.0",
    "convex": "^1.17.0"
  }
}
